import '../css/enterButton.scss'

const EnterButton = ({ onClick }) => {
  return (
    <button className="enter-button-wrapper  delayed-animation">
      Jump in !
    </button>
  )
}

export default EnterButton
